#include <stdio.h>
/**
 * main - Creates an infinite loop
 * Return: 0 (success)
 */
int main(void)
{
int i;

printf("incoming infinite loop :(\n");

i = 0;
/*
 * while (i < 10)
 * {
 *	putchar(i);
 * }
 */
printf("infinite loop avoided! \\o/\n");
return (0);
}
