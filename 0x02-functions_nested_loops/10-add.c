#include "main.h"
/**
 * add - Adds two integers and returns the result
 * @m: The first number for the addition
 * @n: The second number for the addition
 * Return: integer value
 */
int add(int m, int n)
{
return (m + n);
}
