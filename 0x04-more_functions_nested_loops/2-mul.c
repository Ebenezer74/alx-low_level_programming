#include "main.h"
#include <stdio.h>
/**
 * mul - Multiplies two integers
 * @a: The first integer
 * @b: the second integer
 * Return: 0 (Succes)
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
