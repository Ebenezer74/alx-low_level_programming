#include "main.h"
/**
 * _islower - This function checks for lower case characters
 * Return : 0 for lower case characters or 1 otherwise
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
